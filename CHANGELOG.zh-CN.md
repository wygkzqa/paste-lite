# 更新日志

[English](./CHANGELOG.md) | 简体中文

Paste Lite 尚未正式发布，此处汇总当前已支持的功能。

## 未发布

### 修复

- 键盘翻页尚未完成时，后续鼠标选择会保留，不再被旧的翻页请求覆盖或触发滚动。
- 历史面板关闭或重新打开时取消尚未完成的粘贴读取，防止旧请求粘贴到新的目标应用。

### 功能

- **macOS 原生应用** — 支持 macOS 14 及以上版本，常驻菜单栏、不占用 Dock，跟随系统浅色或深色外观。
- **系统打开入口** — 从 Spotlight 或 Finder 启动或再次打开应用时显示主界面；主面板已显示时保留搜索和正在编辑的弹窗，开机启动保持后台常驻。
- **面板收起** — 打开筛选或权限提示后，点击应用外部仍可收起历史面板；编辑和预览弹窗保持原有行为。
- **紧凑布局** — 在主界面底部或设置中切换列表与卡片，选择同步保存；两者均在 macOS 26 使用原生液态玻璃，旧版使用半透明材质，并遵循“减少透明度”设置。原生窗口内容按面板圆角裁切，打开和切换尺寸时刷新阴影，避免外露直角边框。
- **可选渐变背景** — 通用设置可开启「渐变背景」，为两种布局添加蓝紫渐变，默认关闭，自动保存，并适配系统明暗与“减少透明度”设置。
- **选中高亮** — 关闭渐变时，选中记录使用蓝色背景与白色文字；开启渐变时保留半透明高亮，两种布局保持一致。
- **分组管理** — 支持创建、重命名、删除分组及一条记录加入多个分组；右键二级菜单及底部分组菜单直接加入/移出，显示全部或部分归属状态并支持多选批量操作。分组筛选可组合数据库搜索和分页。删除分组保留历史，分组记录沿用现有保留规则。
- **自定义标题** — 通过右键编辑修改文本、链接、图片及文件记录的标题，清空可恢复自动标题；标题持久保存，在列表、卡片及预览中显示，并参与数据库搜索。改名保留原内容与排列；从 Paste 导入保留标题，并为已有记录补入缺失标题，保留本地已有命名。
- **多选与删除** — ⌘单击增减选择，⇧单击/方向键连续选择，⌘A 全选当前筛选下跨页的全部结果；文本输入区域保留文字全选。右键保留已有多选，单条和批量删除均需确认，保留分组定义及外部原文件，仅清理不再被引用的图片资源。
- **记录编辑** — 右键列表项或卡片时先选中记录；编辑与预览共用标题、内容表单，文本和链接支持正文编辑，图片和文件保留原内容。保存同步更新搜索摘要和去重依据，保留时间与分组；内容无效或重复时显示错误并保留输入。预览从右键菜单进入。
- **剪贴板历史** — 在本机保存纯文本、链接、图片和文件引用，支持内容去重，重启后保留历史。
- **搜索与筛选** — 在数据库内搜索全部历史的内容、文件名和来源应用，支持按内容类型与应用筛选；列表分页读取摘要，正文按需加载。新结果返回前保留原列表和选中高亮，相同结果不重复替换，空结果搜索不反复闪现加载动画。
- **缩略图与预览** — 显示图片缩略图，按需打开预览查看图片、文本、链接或文件路径。单个图片文件也参与图片筛选和预览，粘贴时仍保留文件形式。
- **时间标签** — 分段时间标签以打开面板时的时间为基准，不持续刷新计时。
- **快捷键导航** — 使用 ⇧⌘V 打开或关闭历史，↑ / ↓ 选择记录并滚动以保持选中项可见，Return 粘贴选中记录，Esc 关闭面板。
- **复制与粘贴** — 单击选择记录且不触发滚动，双击或 Return 复制并尝试粘贴到原应用。自动粘贴需要辅助功能权限，也可复制后手动按 ⌘V。
- **权限引导** — 从历史面板打开 macOS 辅助功能设置，在返回应用或打开历史时重新检查授权状态。
- **从 Paste 导入** — 在设置的数据分区导入支持的本地历史，保留原始时间、来源应用、Pinboard 名称和记录归属；支持空分组、同名合并、多分组去重及为已有记录补充分组。支持已针对 Paste 6.0.3 验证的数据结构、扫描预览、重复检测、跳过概况、最终保存前取消及失败回滚，不修改源数据。
- **导入条数选择** — 默认导入全部支持的记录；设置条数上限时，可仅导入剩余条数内的最近记录，或提高条数上限，不会为了腾空间而删除已有历史。
- **分栏设置** — 左侧通用、历史、数据、关于菜单，右侧显示对应详情；语言、布局、开机启动及采集大小限制集中到通用。
- **清空历史** — 确认后删除所有历史（含分组内记录）、图片、缩略图和旧迁移备份；保留分组、设置、系统当前剪贴板及原始文件，支持清理失败提示与重试。
- **语言设置** — 默认跟随系统，也可选择简体中文或 English，即时生效并记住选择。通过菜单栏或应用激活时的 ⌘, 打开设置。
- **开机启动** — 在设置中按需开启，由 macOS 登录项管理，支持刷新系统状态，并在需要允许时跳转到系统设置。
- **关于页面** — 在设置的页面标题下方自上而下显示应用 Logo、版本、构建号和 GitHub 仓库入口，保持水平居中；菜单栏的「关于 Paste Lite」直接跳转至同一页面。
- **本地数据管理** — 历史与采集的图片保存在磁盘中，默认不限条数和总容量、永久保留；设置支持文本/图片收录大小、保留天数、最大条数和周期清理，图片处理与存储在后台执行。电脑睡眠或用户会话不活跃时暂停监测，跳过标记为机密、临时或自动生成的剪贴板内容。
