# 更新日志

[English](./CHANGELOG.md) | 简体中文

此处记录面向用户的变化。当前源码版本为 **1.0（构建号 10）**，这是本地开发版本，尚未正式发布。

## 未发布

### 新增

- 使用 SwiftUI 和 AppKit 构建的 macOS 原生菜单栏应用，支持 macOS 14 及以上版本。
- 文本、链接、图片和文件引用的本地剪贴板历史，支持去重和 SwiftData 持久化。
- 全局 ⇧⌘V 快捷键、键盘导航、⌘1–9 操作，以及获得辅助功能权限后的自动粘贴。
- 关键词搜索，以及按内容类型和来源应用筛选。
- 图片缩略图、可选内容预览侧栏和按日期分组的历史列表。
- 后台图片处理与存储、历史容量限制，以及睡眠和会话不活跃时暂停监测。
- 原生应用图标、独立菜单栏图标，以及包含 Logo、版本和构建号的关于窗口。
- 隔离的图片回归测试、中英文项目文档和 MIT 许可证。

### 调整

- 应用显示名称统一为 **Paste Lite**，保留 `PasteLite` 模块名和原有数据目录。
- 分段时间标签在打开列表时计算一次，不再持续刷新计时。
- 单击立即选择记录，取消列表自动滚动；双击仍用于粘贴。
- 单个图片文件可在图片筛选和预览中显示，粘贴时保留文件形式。
- 权限提示中的按钮直接打开辅助功能设置，并在返回应用或重新打开列表时检查授权状态。

### 已知问题

- 系统授权开关已开启时，应用仍可能判断为未授权，目前尚未解决；可在复制后手动按 ⌘V 粘贴。
- 使用临时签名时，替换为新构建可能使旧授权失效。
- 应用界面目前仅提供简体中文。
